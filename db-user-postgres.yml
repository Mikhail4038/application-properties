spring:
  datasource:
    url: jdbc:postgresql://db-user:5432/users
    driver-class-name: org.postgresql.Driver
    username: users 
    password: 500290
  jpa:
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true  